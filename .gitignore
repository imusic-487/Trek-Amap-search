# Node
node_modules/
npm-debug.log*

# Build artifacts（打包产物不入库，registry 从 GitHub Release 拉 zip）
plugin.zip
*.tgz

# TREK SDK 本地开发数据
.trek-dev/

# 临时截图/渲染脚本（docs 下的正式截图保留）
shot_*.js
*.log

dev-fixtures.json
